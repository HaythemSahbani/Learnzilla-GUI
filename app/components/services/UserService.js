'use strict'

angular.module('learnzillaApp')
  .service('UserService', ['$resource', function ($resource) {
    const service = this;
$resource.getRe
    const user = {
      id: null,
      name: '',
      psudoname: '',
      highScore: 0,
      email: ''
    };


    return service;
  }]);
