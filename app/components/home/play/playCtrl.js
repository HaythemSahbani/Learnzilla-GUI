'use strict'

angular.module('learnzillaApp')

    .controller('playCtrl', [  function () {
      const vm = this;
      vm.submitAnswer = function submitAnswer(){
        // make the rest calls
      }
    }
    ]);
